# Resume RAG Assistant 🤖
A conversational AI that answers questions about a resume using **RAG** and **Groq (Llama 3.3)**.

## 🚀 Setup Instructions for GROQ LLM
1. **Clone the repo:** `git clone <your-repo-link>`
2. **Install dependencies:** `pip install -r requirements_groq.txt`
3. **Set up API Key:** Create a `.env` file and add:
   `GROQ_API_KEY=your_key_here`
4. **Run the app:** `python app_groq.py`